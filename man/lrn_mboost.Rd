% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/templates_learners.R
\name{lrn_mboost}
\alias{lrn_mboost}
\title{Gradient boosting learner (mboost)}
\usage{
lrn_mboost(
  name,
  family,
  offset = NULL,
  mstop = 100,
  nu = 0.1,
  frm = NULL,
  max_df = 5,
  knots = 20,
  df_factor = 0.99,
  parameters = NULL
)
}
\arguments{
\item{name}{Character. Name for this learner instance.}

\item{family}{An \pkg{mboost} family object (e.g.
\code{mboost::Gaussian()}, \code{mboost::Binomial(type = "glm")}).
Unlike other \code{lrn_*} learners, \code{"auto"} is not supported
because \pkg{mboost} uses its own family system.}

\item{offset}{Character or \code{NULL}. Column name in \code{x} to use
as an offset.}

\item{mstop}{Integer. Number of boosting iterations. Default \code{100}.}

\item{nu}{Numeric. Shrinkage (learning rate). Default \code{0.1}.}

\item{frm}{Formula or \code{NULL}. If supplied, used as-is.}

\item{max_df}{Integer. Maximum effective degrees of freedom for numeric
smooth base learners. Default \code{5}.}

\item{knots}{Integer. Number of B-spline knots for \code{bbs} base
learners. Default \code{20}.}

\item{df_factor}{Numeric in \eqn{(0, 1]}. Scales the degrees of freedom
relative to the number of unique values. Default \code{0.99}.}

\item{parameters}{An \code{enfold_hyperparameters} object from
\code{\link{specify_hyperparameters}()}, or \code{NULL} (default). When
provided, returns a bare \code{enfold_grid} ready to wrap with a
\code{grd_*()} constructor.}
}
\value{
An \code{enfold_learner} object.
}
\description{
Fits a component-wise gradient boosting model via \code{mboost::mboost}.
Numeric variables are fitted with P-spline base learners (\code{bbs});
binary and categorical variables with linear base learners (\code{bols}).
Requires the \pkg{mboost} package.
}
\examples{
\dontrun{
lrn <- lrn_mboost("boost", family = mboost::Gaussian(), mstop = 200)
fitted <- fit(lrn, x = mtcars[, -1], y = mtcars$mpg)
head(predict(fitted, newdata = mtcars[, -1]))
}
}
\seealso{
\code{\link{lrn_gam}}, \code{\link{lrn_glm}}
}
