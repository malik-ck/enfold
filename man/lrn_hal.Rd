% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/templates_learners.R
\name{lrn_hal}
\alias{lrn_hal}
\title{Highly adaptive LASSO (HAL) learner}
\usage{
lrn_hal(
  name,
  family,
  offset = NULL,
  frm = NULL,
  max_degree = 2,
  smoothness_orders = 1,
  num_knots = 50,
  parameters = NULL
)
}
\arguments{
\item{name}{Character. Name for this learner instance.}

\item{family}{A family object or \code{"auto"}.}

\item{offset}{Character or \code{NULL}. Column name in \code{x} to use
as an offset.}

\item{frm}{Formula or \code{NULL}. Passed to \code{model.matrix()} for
custom basis expansion before fitting.}

\item{max_degree}{Integer. Maximum interaction degree for the HAL basis.
Default \code{2}.}

\item{smoothness_orders}{Integer. Smoothness order for the HAL basis.
Default \code{1}.}

\item{num_knots}{Integer. Number of knots per basis function. Default
\code{50}.}

\item{parameters}{An \code{enfold_hyperparameters} object from
\code{\link{specify_hyperparameters}()}, or \code{NULL} (default). When
provided, returns a bare \code{enfold_grid} ready to wrap with a
\code{grd_*()} constructor.}
}
\value{
An \code{enfold_list} object. Fitting it
creates 100 learners, one for each value of \eqn{\lambda} in the regularization path.
}
\description{
Fits a Highly Adaptive Lasso via \code{hal9001::fit_hal}. Predictors are
internally normalized and can optionally be expanded by a user-supplied
formula. Requires the \pkg{hal9001} package.
}
\examples{
\dontrun{
lrn <- lrn_hal("hal", family = gaussian(), max_degree = 1)
fitted <- fit(lrn, x = as.matrix(mtcars[, -1]), y = mtcars$mpg)
head(predict(fitted, newdata = as.matrix(mtcars[, -1])))
}
}
\seealso{
\code{\link{lrn_glmnet}}, \code{\link{lrn_glm}}
}
