% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/templates_basis_expanders.R
\name{bex_ranger}
\alias{bex_ranger}
\title{Random forest tree-prediction basis expander}
\usage{
bex_ranger(
  name,
  num.trees = 100L,
  mtry = NULL,
  min.node.size = NULL,
  parameters = NULL
)
}
\arguments{
\item{name}{Character scalar. Name of the expander.}

\item{num.trees}{Positive integer. Number of trees. Default \code{100}.}

\item{mtry}{Positive integer or \code{NULL}. Number of variables to
consider at each split; \code{NULL} lets \pkg{ranger} use its default
(floor of sqrt of number of variables for classification, one third for
regression).}

\item{min.node.size}{Positive integer or \code{NULL}. Minimum node size;
\code{NULL} uses the ranger default.}

\item{parameters}{An \code{enfold_hyperparameters} object from
\code{\link{specify_hyperparameters}()}, or \code{NULL} (default). When
provided, returns a bare \code{enfold_grid} ready to wrap with a
\code{grd_*()} constructor.}
}
\value{
An \code{enfold_learner} whose \code{predict()} method returns an
\eqn{n \times \text{num.trees}} numeric matrix of individual tree
predictions.
}
\description{
Trains a \pkg{ranger} random forest and expands the predictor matrix by
replacing it with the individual tree predictions: each tree contributes one
column. A subsequent learner can then weight the trees
optimally. This is sometimes called \emph{random forest stacking} or
\emph{forest embedding}.
}
\examples{
\dontrun{
bex <- bex_ranger("rf_emb", num.trees = 50L)
fitted <- fit(bex, x = mtcars[, -1], y = mtcars$mpg)
dim(predict(fitted, newdata = mtcars[, -1]))  # n x 50
}
}
\seealso{
\code{\link{bex_splines}}, \code{\link{make_pipeline}}
}
