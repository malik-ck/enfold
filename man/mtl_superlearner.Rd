% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/templates_metalearners.R
\name{mtl_superlearner}
\alias{mtl_superlearner}
\title{SuperLearner metalearner (Frank-Wolfe optimization)}
\usage{
mtl_superlearner(
  name,
  loss_fun = loss_gaussian(),
  max_iter = 1000,
  tol = 1e-07,
  parameters = NULL
)
}
\arguments{
\item{name}{Character. Name of the metalearner.}

\item{loss_fun}{An \code{mtl_loss} object with a gradient function
(\code{grad_fun}) (default \code{loss_gaussian()}). The gradient is
required for the Frank-Wolfe update step.}

\item{max_iter}{Integer. Maximum number of Frank-Wolfe iterations.
Default \code{1000}.}

\item{tol}{Numeric. Convergence tolerance (Frank-Wolfe gap). Default
\code{1e-7}.}

\item{parameters}{An \code{enfold_hyperparameters} object from
\code{\link{specify_hyperparameters}()}, or \code{NULL} (default). When
provided, returns a bare \code{enfold_grid} ready to wrap with a
\code{grd_*()} constructor.}
}
\value{
An \code{enfold_learner} object.
}
\description{
Learns a convex combination of base learner predictions that minimizes the
given loss function using the Frank-Wolfe algorithm. The resulting weights
are non-negative and sum to one.
}
\examples{
\dontrun{
x <- mtcars[, -1]; y <- mtcars$mpg
task <- initialize_enfold(x, y) |>
  add_learners(lrn_glm("glm", family = gaussian()), lrn_mean("mean")) |>
  add_metalearners(mtl_superlearner("sl")) |>
  add_cv_folds(inner_cv = 5L, outer_cv = NA) |>
  fit()
}
}
\seealso{
\code{\link{mtl_selector}}, \code{\link{loss_gaussian}}
}
