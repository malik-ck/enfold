% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructors_grids.R
\name{change_arguments}
\alias{change_arguments}
\title{Modify hyperparameters of a learner or a node inside a pipeline}
\usage{
change_arguments(object, directory, combo, name = NULL)
}
\arguments{
\item{object}{An \code{enfold_learner} or \code{enfold_pipeline}.}

\item{directory}{\code{NULL} for a plain learner; a non-empty character
vector navigating to the target node for pipelines.}

\item{combo}{A named list of hyperparameter values to apply. Merged into
the existing parameters via \code{modifyList}.}

\item{name}{Optional new name for the returned object.}
}
\value{
A modified copy of \code{object}.
}
\description{
Returns a modified copy of \code{object} with hyperparameters updated
according to \code{combo}. Does not mutate the original.
}
\details{
For a plain \code{enfold_learner}, \code{directory} must be \code{NULL}.
For an \code{enfold_pipeline}, \code{directory} is a character vector
giving the name of the target node (and optionally the names of nested
nodes for multi-level pipelines, e.g. \code{c("outer", "inner")}).
}
