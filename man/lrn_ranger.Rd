% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/templates_learners.R
\name{lrn_ranger}
\alias{lrn_ranger}
\title{Random forest learner (ranger back-end)}
\usage{
lrn_ranger(
  name,
  select_vars = NULL,
  num.trees = 500,
  mtry = NULL,
  probability = FALSE,
  min.node.size = NULL,
  min.bucket = NULL,
  max.depth = NULL,
  splitrule = NULL,
  parameters = NULL
)
}
\arguments{
\item{name}{Character. Name for this learner instance.}

\item{select_vars}{Character vector or \code{NULL}. If provided, only these
columns of \code{x} are used.}

\item{num.trees}{Integer. Number of trees. Default \code{500}.}

\item{mtry}{Integer or \code{NULL}. Number of variables to try at each
split; \code{NULL} uses the ranger default.}

\item{probability}{Logical. If \code{TRUE}, fits a probability forest
(suitable for binary outcomes). Default \code{FALSE}.}

\item{min.node.size}{Integer or \code{NULL}. Minimum node size.}

\item{min.bucket}{Integer or \code{NULL}. Minimum bucket size (terminal
node observations).}

\item{max.depth}{Integer or \code{NULL}. Maximum tree depth.}

\item{splitrule}{Character or \code{NULL}. Splitting rule passed to
\code{ranger}.}

\item{parameters}{An \code{enfold_hyperparameters} object from
\code{\link{specify_hyperparameters}()}, or \code{NULL} (default). When
provided, returns a bare \code{enfold_grid} ready to wrap with a
\code{grd_*()} constructor.}
}
\value{
An \code{enfold_learner} object.
}
\description{
Fits a random forest via \code{ranger::ranger}. Requires the \pkg{ranger}
package.
}
\examples{
\dontrun{
lrn <- lrn_ranger("rf", num.trees = 200L)
fitted <- fit(lrn, x = mtcars[, -1], y = mtcars$mpg)
head(predict(fitted, newdata = mtcars[, -1]))
}
}
\seealso{
\code{\link{lrn_gam}}, \code{\link{grd_random}}
}
