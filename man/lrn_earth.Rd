% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/templates_learners.R
\name{lrn_earth}
\alias{lrn_earth}
\title{Multivariate adaptive regression splines (MARS) learner}
\usage{
lrn_earth(
  name,
  family,
  offset = NULL,
  degree = 2,
  penalty = 3,
  nk = 100,
  thresh = 0.01,
  pmethod = "backward",
  nfold = 0,
  parameters = NULL
)
}
\arguments{
\item{name}{Character. Name for this learner instance.}

\item{family}{A family object, \code{"auto"}, or \code{NULL} for a plain
(non-GLM) MARS model.}

\item{offset}{Character or \code{NULL}. Column name in \code{x} to use
as an offset.}

\item{degree}{Integer. Maximum interaction degree. \code{1} = additive
(no interactions). Default \code{2}.}

\item{penalty}{Numeric. GCV penalty per knot. Default \code{3}.}

\item{nk}{Integer. Maximum number of knots before pruning. Default
\code{100}.}

\item{thresh}{Numeric. Forward pass stopping threshold. Default \code{0.01}.}

\item{pmethod}{Character. Pruning method passed to \code{earth}. Default
\code{"backward"}.}

\item{nfold}{Integer. Number of cross-validation folds internal to
\code{earth} (\code{0} disables internal CV). Default \code{0}.}

\item{parameters}{An \code{enfold_hyperparameters} object from
\code{\link{specify_hyperparameters}()}, or \code{NULL} (default). When
provided, returns a bare \code{enfold_grid} ready to wrap with a
\code{grd_*()} constructor.}
}
\value{
An \code{enfold_learner} object.
}
\description{
Fits a MARS model via \code{earth::earth}. Supports all GLM families.
Requires the \pkg{earth} package.
}
\examples{
\dontrun{
lrn <- lrn_earth("mars", family = gaussian(), degree = 2)
fitted <- fit(lrn, x = mtcars[, -1], y = mtcars$mpg)
head(predict(fitted, newdata = mtcars[, -1]))
}
}
\seealso{
\code{\link{lrn_gam}}, \code{\link{lrn_glm}}
}
