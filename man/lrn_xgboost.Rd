% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/templates_learners.R
\name{lrn_xgboost}
\alias{lrn_xgboost}
\title{XGBoost learner with optional snapshotting}
\usage{
lrn_xgboost(
  name,
  select_vars = NULL,
  nrounds = 200L,
  n_snapshots = NULL,
  max_depth = 3L,
  eta = 0.1,
  objective = "reg:squarederror",
  nthread = 1L,
  verbose = 0L,
  lambda = 2,
  alpha = 0,
  min_child_weight = 5
)
}
\arguments{
\item{name}{Character scalar. Name of the learner.}

\item{select_vars}{Column indices or names to subset \code{x} before
fitting, or \code{NULL} (default) to use all columns.}

\item{nrounds}{Positive integer. Total number of boosting rounds.}

\item{n_snapshots}{Positive integer. Number of evenly-spaced rounds at
which a prediction column is generated.  Default is \code{NULL}, which means no snapshotting.
n_snapshots == 1 is treated the same as no snapshotting.}

\item{max_depth}{Positive integer. Maximum tree depth.}

\item{eta}{Numeric. Learning rate (step-size shrinkage).}

\item{objective}{Character. XGBoost objective string, e.g.
\code{"reg:squarederror"} (default), \code{"binary:logistic"}.}

\item{nthread}{Positive integer. Number of parallel threads.}

\item{verbose}{Integer. Verbosity level passed to \code{xgb.train}:
\code{0} = silent (default).}

\item{lambda}{Numeric. L2 regularisation weight on leaf weights.}

\item{alpha}{Numeric. L1 regularisation weight on leaf weights. Default \code{0}.}

\item{min_child_weight}{Numeric. Minimum sum of instance weight in a leaf.}
}
\value{
An \code{enfold_list} whose \code{predict()} method returns a named
list with one numeric vector per snapshot round.
}
\description{
Trains a gradient boosted tree model via \code{xgboost::xgb.train} and
returns one prediction column per snapshot. Snapshots are taken at evenly-spaced boosting rounds
(the final snapshot is always at \code{nrounds}), so a downstream
metalearner can weight the rounds optimally.
}
\examples{
\dontrun{
lrn <- lrn_xgboost("xgb", nrounds = 100L, n_snapshots = 5L)
fitted <- fit(lrn, x = as.matrix(mtcars[, -1]), y = mtcars$mpg)
preds <- predict(fitted, newdata = as.matrix(mtcars[, -1]))
length(preds)  # 5 snapshots
}
}
\seealso{
\code{\link{lrn_ranger}}, \code{\link{lrn_glmnet}}
}
