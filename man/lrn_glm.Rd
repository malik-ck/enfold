% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/templates_learners.R
\name{lrn_glm}
\alias{lrn_glm}
\title{Generalized linear model learner}
\usage{
lrn_glm(name, family, offset = NULL, parameters = NULL)
}
\arguments{
\item{name}{Character. Name for this learner instance.}

\item{family}{A family object (e.g. \code{gaussian()}, \code{binomial()},
\code{poisson()}) or the string \code{"auto"} to let the learner guess
from \code{y}.}

\item{offset}{Character or \code{NULL}. Name of a column in \code{x} to
use as an offset.}

\item{parameters}{An \code{enfold_hyperparameters} object from
\code{\link{specify_hyperparameters}()}, or \code{NULL} (default). When
provided, returns a bare \code{enfold_grid} ready to wrap with a
\code{grd_*()} constructor.}
}
\value{
An \code{enfold_learner} object.
}
\description{
Fits a GLM via \code{stats::glm}. Handles data frames with factor and
character columns automatically.
}
\examples{
lrn <- lrn_glm("glm_gauss", family = gaussian())
fitted <- fit(lrn, x = mtcars[, -1], y = mtcars$mpg)
head(predict(fitted, newdata = mtcars[, -1]))
}
\seealso{
\code{\link{lrn_gam}}, \code{\link{lrn_glmnet}},
\code{\link{make_learner_factory}}
}
