% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructors_hyperparameters.R
\name{log_transform}
\alias{log_transform}
\alias{log_transform.enfold_hyperparameters}
\title{Log-scale sampling for continuous hyperparameters}
\usage{
log_transform(obj, ...)

\method{log_transform}{enfold_hyperparameters}(obj, ...)
}
\arguments{
\item{obj}{An object of class \code{enfold_hyperparameters}.}

\item{...}{Unquoted names of continuous hyperparameters to log-transform.
Each referenced parameter must be an \code{enfold_range} with
\eqn{\text{min} > 0}.}
}
\value{
An updated \code{enfold_hyperparameters} object.
}
\description{
Causes the specified continuous (\code{enfold_range}) hyperparameters to be
sampled on the log scale, so that \code{draw()} produces values
\eqn{\exp(U[\log(\text{min}), \log(\text{max})])} rather than
\eqn{U[\text{min}, \text{max}]}.
}
\examples{
params <- specify_hyperparameters(lambda = make_range(1e-4, 10)) |>
  log_transform(lambda)

# Draws are now log-uniformly distributed
draw(params, n = 5L)
}
\seealso{
\code{\link{specify_hyperparameters}}, \code{\link{make_range}}
}
