% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/templates_learners.R
\name{lrn_glmnet}
\alias{lrn_glmnet}
\title{Elastic net / LASSO regularization path learner}
\usage{
lrn_glmnet(name, family, offset = NULL, frm = NULL, alpha = 1, nlambda = 20)
}
\arguments{
\item{name}{Character. Name for this learner instance.}

\item{family}{A family object or \code{"auto"}.}

\item{offset}{Character or \code{NULL}. Column name in \code{x} to use
as an offset.}

\item{frm}{Formula or \code{NULL}. Passed to \code{model.matrix()} for
custom basis expansion.}

\item{alpha}{Numeric in \eqn{[0, 1]}. Elastic net mixing parameter.
\code{1} (default) = LASSO, \code{0} = ridge.}

\item{nlambda}{Integer. Number of \eqn{\lambda} values in the path.
Default \code{20}.}
}
\value{
An \code{enfold_list} learner (a subclass of
\code{enfold_learner}) whose \code{predict()} method returns a named
list with one entry per \eqn{\lambda}.
}
\description{
Fits an entire regularization path via \code{glmnet::glmnet} and returns
predictions for each \eqn{\lambda} value as a named list. Because it
returns multiple outputs it is an \code{enfold_list} learner — each
\eqn{\lambda} column is treated as an independent learner by the
metalearner. Predictors are internally centered and scaled. Requires the
\pkg{glmnet} package.
}
\examples{
\dontrun{
lrn <- lrn_glmnet("en", family = gaussian(), alpha = 0.5)
fitted <- fit(lrn, x = as.matrix(mtcars[, -1]), y = mtcars$mpg)
preds <- predict(fitted, newdata = as.matrix(mtcars[, -1]))
length(preds)  # nlambda entries
}
}
\seealso{
\code{\link{lrn_bigGlm}}, \code{\link{lrn_hal}}
}
