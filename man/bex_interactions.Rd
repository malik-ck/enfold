% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/templates_basis_expanders.R
\name{bex_interactions}
\alias{bex_interactions}
\title{Interaction basis expander}
\usage{
bex_interactions(name, depth = 2L, parameters = NULL)
}
\arguments{
\item{name}{Character scalar. Name of the expander.}

\item{depth}{Positive integer. Maximum interaction order.  \code{2}
(default) adds all pairwise products; \code{3} additionally adds all
triple products, and so on.  For most SuperLearner use cases \code{2}
is plenty.}

\item{parameters}{An \code{enfold_hyperparameters} object from
\code{\link{specify_hyperparameters}()}, or \code{NULL} (default). When
provided, returns a bare \code{enfold_grid} ready to wrap with a
\code{grd_*()} constructor.}
}
\value{
An \code{enfold_learner} whose \code{predict()} method returns a
numeric matrix containing main effects and all interaction columns.
}
\description{
Expands the predictor matrix to include all pairwise (or higher-order) interaction terms up to the specified depth.
The column set produced on the training data is recorded and enforced at prediction time.
}
\examples{
bex <- bex_interactions("inter2", depth = 2L)
fitted <- fit(bex, x = as.matrix(mtcars[, -1]), y = mtcars$mpg)
dim(predict(fitted, newdata = as.matrix(mtcars[, -1])))
}
\seealso{
\code{\link{bex_splines}}, \code{\link{bex_formula}},
\code{\link{make_pipeline}}
}
